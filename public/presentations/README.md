# Published presentation snapshots

These three HTML files preserve published Quarto slide content and link to their original assets through a base URL. The source QMD files live in [Brian’s Presentations repository](https://github.com/bcervantesalvarez/Presentations). Refresh snapshots when those sources are re-rendered, retaining the integration fixes below.

- BuildAQuartoPortfolio: omit four unavailable decorative Tenor GIFs (five image references).
- TeachingQuarto: correct the case-sensitive `Quarto.jpg` image reference. Preserve the Python example as code and link its live demonstration to the original presentation; its service worker requires that original origin.
- MissingData: identify three absent plot images explicitly. Their files are missing from the published repository; no chart values were recreated.

LoanApproval and WebR remain linked to their self-contained published HTML. The local water-parasites deck is a Slides.com export with a repaired opening HTML tag and descriptive document title. No QMD source exists for that export in this repository.

AI contribution: Codex adjusted integration, asset references, and missing-image notices. Existing slide content remains supplied by the original authors. Model/version: unknown (metadata unavailable). Reasoning effort: unknown (metadata unavailable). No human signoff recorded.
