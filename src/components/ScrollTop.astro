<button id="scroll-top" type="button" aria-label="Back to top" title="Back to top" hidden>
  <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M12 19V5M5 12l7-7 7 7" /></svg>
</button>
<script>
  import { mountNavigation } from '../lib/navigation.js';
  mountNavigation(window, document);
</script>
<style>
  #scroll-top { position: fixed; left: max(12px,env(safe-area-inset-left)); bottom: max(16px,env(safe-area-inset-bottom)); z-index: 40; display: grid; place-items: center; width: 48px; height: 48px; border: 2px solid var(--color-bg); border-radius: 50%; color: var(--color-bg); background: var(--color-ink); box-shadow: 0 4px 16px rgb(0 0 0 / .25); cursor: pointer; }
  #scroll-top[hidden] { display: none; }
  #scroll-top:hover { background: var(--color-accent-ink); }
  #scroll-top:focus-visible { outline: 3px solid var(--color-accent); outline-offset: 3px; }
</style>
